export const player = (name, server, region, job, isTrial) => [
    {
        "name": name,
        "server": server,
        "region": region,
        "job": job,
        "isTrial": isTrial
    }
]

export default player;